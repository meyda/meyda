// const chai = require('chai');
// const assert = chai.assert;

// Setup
// const meyda = require('../dist/node/main');

describe('main', () => {
  it('should call an extractor when asked to', (done) => {
    done();
  });
});
